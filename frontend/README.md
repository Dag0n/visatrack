# VisaTrack UK frontend

Vite + React app. See the [root README](../README.md) for full dev/build
instructions covering both the frontend and backend.

Quick start:

```bash
npm install
cp .env.example .env
npm run dev
```
